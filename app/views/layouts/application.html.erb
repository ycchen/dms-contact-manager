<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewpoint" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Contacts Manager" %></title>
	<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	
  <%= csrf_meta_tags %>
</head>
<body>
	<!-- navigation -->
	<%= render :partial => 'shared/navigation' %>

	<!-- main container -->
	<div class="container">
		<div class="flashes">
			<%= flashes_helper %>		
		</div>
		<div class="content">
			<div class="row">
				<div class="span12">
					<div class="main-image"><%= image_tag '/assets/background-11.jpg' %></div>	
					<%= yield %>

					<br/>

					<%#= debug(params) if Rails.env.development? %>
					<%#= debug(RUBY_PLATFORM) %>
					<%#= debug("Page Rendered in #{ sprintf('%.3f', (Time.now.usec - @start_time).abs / 1000000.0) } seconds)") %>
				</div>	
			</div>	
		</div>
	</div>
	<%#= yield :scripts %>
	
	<%= javascript_include_tag "application", "data-turbolinks-track" => true %>

</body>
</html>
